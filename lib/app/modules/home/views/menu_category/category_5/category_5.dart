import 'package:flutter/material.dart';

class CategoryFive extends StatelessWidget {
  const CategoryFive({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Category Five"),
    );
  }
}
