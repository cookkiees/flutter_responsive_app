import 'package:flutter/material.dart';

class CategoryFour extends StatelessWidget {
  const CategoryFour({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Category Four"),
    );
  }
}
