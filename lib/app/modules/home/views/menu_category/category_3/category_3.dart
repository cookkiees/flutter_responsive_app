import 'package:flutter/material.dart';

class CategoryThree extends StatelessWidget {
  const CategoryThree({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Category Tree"),
    );
  }
}
